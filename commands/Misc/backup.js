const axios = require('axios')

module.exports = {
    name: 'test',
    aliases: [''],

    /**
     * @param {Message} message
     * @param {Client} client
     * @param {String[]} args
     */
     execute: async(client, message, args) => {
    }
}